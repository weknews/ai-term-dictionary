# 용어집 갱신 규칙

## 용어 추가·수정
`terms.js`만 고친다. `index.html`은 건드릴 일이 없다.

```js
{
  k:"컨텍스트 엔지니어링",              // 한글 표기 (없으면 원어 음차)
  e:"Context Engineering",            // 영문/원어
  c:"prompt",                         // 분류 키 (CATS 참고)
  y:"2025~",                          // 등장·확산 시기
  d:"한두 문장 정의.",                  // 정의 — 사실만
  n:"업계에서 이 말을 어떻게 받아들이는지.", // 뉘앙스 (선택) — 이 줄이 이 문서의 존재 이유
  s:"지금 이 용어가 어느 단계에 있는지.",   // 관찰 시점 현황 (선택) — 날짜가 박히는 줄
  f:"hot"                             // 플래그 (선택)
}
```

## 플래그
| 값 | 뜻 | 붙이는 기준 |
|---|---|---|
| `hot` | 지금 뜨는 말 | 최근 1년 안에 확산됐고, 알면 최신 흐름을 아는 사람으로 읽히는 말 |
| `care` | 잘못 쓰면 까임 | 뜻이 흔히 오용되거나, 뉘앙스를 모르고 쓰면 신뢰를 잃는 말 |
| `old` | 쓰면 옛날 사람 | 한때 유행했으나 지금 진지하게 꺼내면 시대에 뒤처져 보이는 말 |

플래그는 남발하지 않는다. 전체의 3분의 1을 넘으면 의미가 없어진다.

## 시점 표기 규칙
이 씬에서 "2026년 기준"은 이미 너무 넓은 표현이다. 분기 단위로 뜻과 위상이 바뀐다.

- 시점에 따라 달라지는 설명은 `d`나 `n`이 아니라 **`s`에만** 적는다.
- `s`는 화면에 `index.html`의 `ASOF` 상수 날짜와 함께 렌더링된다. 갱신할 때 이 값과 상단 배지 날짜를 같이 올린다.
- `s`가 붙은 항목은 "시점 의존" 필터로 모아 볼 수 있다. **갱신 회차마다 이 목록부터 훑는다.**
- 반대로 시간이 지나도 안 변할 설명(누가 언제 만들었나, 무슨 뜻인가)에는 `s`를 붙이지 않는다. 남발하면 매번 44개가 아니라 236개를 재검토해야 한다.

## 정의(d) · 뉘앙스(n) · 현황(s)의 분리
- `d`는 검증 가능한 사실만. 누가 언제 만들었고 무엇을 뜻하는지.
- `n`은 판단이 들어가도 된다. 어디서 쓰면 안 되는지, 무슨 질문이 따라오는지, 우리 상황에 어떤 의미인지.
- `s`는 관찰 날짜가 붙는 줄이다. 지금 이게 표준인지, 연구 단계인지, 한물갔는지.
- 셋을 섞으면 이 문서가 그냥 또 하나의 위키가 된다.

## 표현 원칙
누구를 깎아내리는 문장은 쓰지 않는다. "모르면 까인다"가 아니라 "이렇게 말하면 설명이 덜 필요하다"로 적는다.
목적은 구성원을 훈계하는 게 아니라 같은 단어로 같은 얘기를 하게 만드는 것이다.

## 발행
1. `terms.js` 수정
2. `index.html` 상단 배지의 버전·날짜, 변경 이력 섹션에 항목 추가
3. 커밋 후 아티팩트 재발행 (같은 URL로 갱신됨)

## 버전 기준
- **major** — 분류 체계나 문서 구조를 바꿀 때
- **minor** — 용어를 묶음으로 추가하거나 뉘앙스를 크게 손볼 때
- **patch** — 오타, 개별 용어 한두 개 수정

## "어디까지 왔나" 섹션 갱신
이 섹션의 유통기한이 문서에서 가장 짧다. **한 달 주기로 다시 훑는다.**

수집 경로 (전부 인증 없이 됨):

```bash
# Hacker News — 최근 인기 스토리
curl -sL "https://hn.algolia.com/api/v1/search_by_date?tags=story&numericFilters=points%3E200,created_at_i%3E$(date -v-14d +%s)&hitsPerPage=30"

# HN — 특정 주제 반응 (점수·댓글수로 화제성 판단)
curl -sL "https://hn.algolia.com/api/v1/search?query=<키워드>&tags=story&numericFilters=points%3E20"

# GitHub — 최근 신규 저장소 스타 순
curl -sL "https://api.github.com/search/repositories?q=created:%3E$(date -v-30d +%F)+stars:%3E500&sort=stars&order=desc&per_page=25"

# Reddit — 커뮤니티 상위 (plain curl은 403, curl_cffi 지문 필요)
python3 -c "from curl_cffi import requests as r; print(r.get('https://www.reddit.com/r/LocalLLaMA/hot/.rss', impersonate='safari').text)"
```

### 수록 기준
- **소문·유출·예고는 뺀다.** r/singularity 상위권 절반이 이런 것들이다. 화제성 신호로만 쓰고 본문에 쓰지 않는다.
- **벤더 발표 수치는 그렇다고 표시한다.** "회사 발표 기준"이라고 적고, 자체 검증 전에는 근거로 쓰지 말라고 덧붙인다.
- **모델 이름은 최소한으로.** 주 단위로 낡는다. 구조적 서술("플래그십은 100만 토큰이 표준")로 바꿔 쓸 수 있으면 그렇게 한다.
- 새 범주가 생겼으면 `terms.js`에도 용어로 넣는다. 섹션과 사전이 따로 놀면 안 된다.
