# 용어집 갱신 규칙

## 용어 추가·수정
`terms.js`만 고친다. `index.html`은 건드릴 일이 없다.

```js
{
  k:"컨텍스트 엔지니어링",              // 한글 표기 (없으면 원어 음차)
  e:"Context Engineering",            // 영문/원어
  c:"prompt",                         // 분류 키 (CATS 참고)
  y:"2025~",                          // 등장·확산 시기
  d:"한두 문장 정의.",                  // 정의 — 사실만
  n:"업계에서 이 말을 어떻게 받아들이는지.", // 뉘앙스 (선택) — 이 줄이 이 문서의 존재 이유
  s:"지금 이 용어가 어느 단계에 있는지.",   // 관찰 시점 현황 (선택) — 날짜가 박히는 줄
  f:"hot"                             // 플래그 (선택)
}
```

## 플래그
| 값 | 뜻 | 붙이는 기준 |
|---|---|---|
| `hot` | 지금 뜨는 말 | 지금 대화에서 비중이 큰 말. **등장 연도 기준이 아니다** — 몇 년 전 나왔어도 이제야 중요해졌으면 여기 해당한다. 등장 시기는 `y` 필드가 따로 보여준다 |
| `care` | 쓸 때 주의 | 뜻이 흔히 오용되거나, 뉘앙스를 모르고 쓰면 설명이 한 번 더 필요해지는 말 |
| `old` | 한물간 말 | 한때 널리 쓰였으나 지금 그대로 꺼내면 오래된 자료처럼 읽히는 말 |

플래그는 남발하지 않는다. 전체의 3분의 1을 넘으면 의미가 없어진다.
현재 분포는 hot 41 · care 28 · old 4 · 표시 없음 172다. 대부분이 표시 없음인 게 정상이다 —
플래그는 예외를 표시하는 장치라서, 많아지면 아무것도 표시하지 않는 것과 같아진다.

`old`가 유독 적은 것은 누락이 아니다. 이 씬은 말을 폐기하는 대신 지위를 바꾼다.
프롬프트 엔지니어링은 직업에서 기본 소양으로 강등됐고, 사고 사슬은 모델 안으로 흡수됐다.
진짜 폐어는 실패한 제품(AutoGPT)과 다른 동네에서 빌려온 말(세컨드 브레인) 정도다.
새 용어에 `old`를 달기 전에 "정말 안 쓰는가, 아니면 지위만 바뀌었는가"를 먼저 따진다.

## 수록 범위 — AI 용어집이다
용어를 넣기 전에 묻는다. **AI 때문에 이 말이 문제가 되는가.**
AI가 없어도 원래 있던 말이면 여기 들어갈 자리가 아니다. 우리 업무에 필요한 말이라는 것과
AI 용어라는 것은 다르다.

판정이 갈렸던 예:
- `변경관리 계획서(PCCP)` — 들어간다. 모델을 업데이트하면 허가가 깨지는 문제를 푸는 제도라 순수 AI 이슈다.
- `신의료기술평가` — 들어간다. 허가와 사용 승인이 별개라는 함정에 AI 제품이 특히 많이 걸린다.
- `PACS` — 들어간다. 영상 AI 제품의 도입 가부를 실제로 가르는 것이 연동이다.
- `IRB`, `HL7`, `SNOMED CT`, `청구 데이터`, `식약처` — 뺐다. AI가 없어도 원래 있던 말이다.
  필요하면 의료기기 사업 자료에 따로 정리한다.

같은 기준이 다른 분야에도 적용된다. 클라우드·보안·통계 일반 용어를
"우리 일에 필요하니까"로 끌어들이면 문서의 성격이 흐려진다.

## 출처 없이 쓰지 않는다
**이 문서에서 가장 많이 난 사고가 이것이다.** 2025년 이후 등장한 용어는 뜻이 빠르게 넓어지거나
옮겨가서, 아는 대로 쓰면 한두 해 전 의미에 머무른다.

실제 사례:
- `그래프 엔지니어링`을 특정 오케스트레이션 도구를 쓰는 방식이라고 좁혀 썼다.
  지금은 프롬프트 → 컨텍스트 → 하네스 → 루프 다음에 오는 시스템 설계 방법론이다.
- `에이전트 함대`를 "물량으로 밀어붙여 성공한 경로만 건진다"로 썼다. 그건 스웜 쪽이고,
  함대는 수백~수천 개를 임무 범위로 묶어 다루는 운영 단위다.

**규칙**: `y`가 2025 이후인 용어는 정의를 쓰기 전에 반드시 검색한다.
원문(발표 블로그, 논문 초록, 명세)을 최소 하나 확인하고, 2차 요약만 보고 쓰지 않는다.
확인 못 했으면 `n`에 "쓰는 사람마다 범위가 다르다"처럼 불확실성을 적고 단정하지 않는다.

```bash
# 2025년 이후 등장 용어 목록 — 갱신 회차마다 출처 확인 대상
node -e "global.window={};require('./terms.js');
console.log(window.TERMS.filter(t=>/202[5-9]/.test(t.y)).map(t=>t.k).join(', '))"
```

## 서식 금지
`terms.js`의 `d`·`n`·`s`는 **순수 텍스트만** 넣는다. `<b>` 같은 HTML 태그를 쓰면 화면에 태그가 그대로 찍힌다
(렌더러가 검색어 강조를 위해 모든 입력을 이스케이프한다). 강조가 필요하면 문장 구조로 해결한다.

```bash
node -e "global.window={};require('./terms.js');
window.TERMS.forEach(t=>['d','n','s'].forEach(f=>{if(t[f]&&/<[a-z\/]/i.test(t[f]))console.log(t.k,f)}))"
```

## 시점 표기 규칙
이 씬에서 "2026년 기준"은 이미 너무 넓은 표현이다. 분기 단위로 뜻과 위상이 바뀐다.

- 시점에 따라 달라지는 설명은 `d`나 `n`이 아니라 **`s`에만** 적는다.
- `s`는 화면에 `index.html`의 `ASOF` 상수 날짜와 함께 렌더링된다. 갱신할 때 이 값과 상단 배지 날짜를 같이 올린다.
- `s`가 붙은 항목은 사이드바 **설명의 수명 → 시점 의존** 필터로 모아 볼 수 있다. **갱신 회차마다 이 목록부터 훑는다.**
- 이 필터는 플래그가 아니다. hot/care/old는 *단어의 성격*을, 시점 의존은 *설명의 수명*을 가리킨다.
  사이드바에서도 두 묶음으로 나눠 놨으니 새 표시를 만들 때 어느 쪽에 속하는지 먼저 정한다.
- 반대로 시간이 지나도 안 변할 설명(누가 언제 만들었나, 무슨 뜻인가)에는 `s`를 붙이지 않는다. 남발하면 매번 44개가 아니라 236개를 재검토해야 한다.

## 정의(d) · 뉘앙스(n) · 현황(s)의 분리
- `d`는 검증 가능한 사실만. 누가 언제 만들었고 무엇을 뜻하는지.
- `n`은 판단이 들어가도 된다. 어디서 쓰면 안 되는지, 무슨 질문이 따라오는지, 우리 상황에 어떤 의미인지.
- `s`는 관찰 날짜가 붙는 줄이다. 지금 이게 표준인지, 연구 단계인지, 한물갔는지.
- 셋을 섞으면 이 문서가 그냥 또 하나의 위키가 된다.

## 표현 원칙

### 같은 말을 버릇처럼 쓰지 않는다
글을 많이 쓰다 보면 특정 추상명사에 손이 간다. `자리` `지점` `대목` `축` `카드` `장치` `관문`
`~하는 게 정석이다` `~라는 게 중론이다` 같은 것들이다. 한두 번은 자연스럽지만
문서 전체에 깔리면 번역투처럼 읽힌다.

```bash
# 반복 빈도 점검 — 1만자당 5회를 넘으면 들여다볼 것
node -e "global.window={};require('./terms.js');
const T=window.TERMS.map(t=>[t.d,t.n,t.s].filter(Boolean).join(' ')).join(' ');
['자리','지점','대목','장치','관문','정석','카드','중론','전제','셈이다'].forEach(w=>{
  const n=(T.match(new RegExp(w,'g'))||[]).length;
  if(n) console.log(w, n, (n/T.length*10000).toFixed(1)+'/만자');
})"
```

숫자를 보고 바로 고치지 말고 문장을 열어본다. 같은 글자라도 뜻이 다르면 반복이 아니다
(`안전장치` · `막는 장치` · `고르는 장치`는 서로 다른 말이고, `자리잡다`는 동사다).
고칠 것은 **뜻을 뭉뚱그리려고 끌어다 쓴 경우**다.

### 읽는 사람을 낮추지 않는다
문서 구조 자체도 표현이다. "핵심만", "쉬운 것부터", "다 읽을 필요는 없다" 같은 틀은
읽는 사람이 전체를 감당 못 한다는 전제를 깔고 시작한다. 분량을 줄여 보여줄 때는
**왜 그렇게 묶었는지**를 능력이 아니라 상황으로 설명한다.

상황 제목도 마찬가지다. "기술팀 얘기를 알아들어야 할 때"는 읽는 사람을
자기 회사 엔지니어 말도 못 알아듣는 자리에 세운다. "만드는 쪽과 얘기할 때"로 적으면
같은 내용인데 양쪽이 대등해진다.

### 사람이 아니라 말을 평가한다
누구를 깎아내리는 문장은 쓰지 않는다. "모르면 까인다"가 아니라 "이렇게 말하면 설명이 덜 필요하다"로 적는다.
목적은 구성원을 훈계하는 게 아니라 같은 단어로 같은 얘기를 하게 만드는 것이다.

쓰지 않는 표현: 촌스럽다 · 아마추어 소리를 듣는다 · 시간이 멈춘 사람이 된다 · 신뢰를 잃는다 ·
업데이트가 안 된 사람으로 보인다 · 바로 알아챈다 · 삽질 · 놈·녀석·따위 같은 구어.

대신 쓰는 표현: 설명이 한 번 더 필요해진다 · 오래된 자료처럼 읽힌다 · 되묻게 된다 ·
근거가 약하다는 인상을 준다 · 시간을 버리게 된다.

### 문서 위치를 가리키지 않는다
**검색하면 항목 하나만 화면에 뜬다.** "아래 사다리", "위 표", "앞서 본" 같은 표현은
그 자리에서 가리킬 대상이 없어 독자가 멈춘다. 다른 항목을 언급해야 하면 이름을 그대로 적는다.

자기 참조도 같다. "이 용어집이 겨냥하는 것"처럼 문서 전체를 가리키는 말은 항목 안에 넣지 않는다.

발행 전 점검:

```bash
# 사람을 깎아내리는 표현
node -e "global.window={};require('./terms.js');
const P=/촌스럽|아마추어|까임|욕이다|시간이 멈춘|신뢰를 잃|삽질|놈|녀석|따위/;
window.TERMS.forEach(t=>['d','n','s'].forEach(f=>{if(t[f]&&P.test(t[f]))console.log(t.k,f)}))"

# 문서 위치 참조
node -e "global.window={};require('./terms.js');
const P=/사다리|치트시트|이 용어집|아래 표|위 표|앞서 본|그 위의/;
window.TERMS.forEach(t=>['d','n','s'].forEach(f=>{if(t[f]&&P.test(t[f]))console.log(t.k,f)}))"
```

## 발행
1. `terms.js` 수정
2. `index.html` 상단 배지의 버전·날짜, 변경 이력 섹션에 항목 추가
3. 커밋 후 아티팩트 재발행 (같은 URL로 갱신됨)

## 버전 기준
- **major** — 분류 체계나 문서 구조를 바꿀 때
- **minor** — 용어를 묶음으로 추가하거나 뉘앙스를 크게 손볼 때
- **patch** — 오타, 개별 용어 한두 개 수정

## "어디까지 왔나" 섹션 갱신
이 섹션의 유통기한이 문서에서 가장 짧다. **한 달 주기로 다시 훑는다.**

수집 경로 (전부 인증 없이 됨):

```bash
# Hacker News — 최근 인기 스토리
curl -sL "https://hn.algolia.com/api/v1/search_by_date?tags=story&numericFilters=points%3E200,created_at_i%3E$(date -v-14d +%s)&hitsPerPage=30"

# HN — 특정 주제 반응 (점수·댓글수로 화제성 판단)
curl -sL "https://hn.algolia.com/api/v1/search?query=<키워드>&tags=story&numericFilters=points%3E20"

# GitHub — 최근 신규 저장소 스타 순
curl -sL "https://api.github.com/search/repositories?q=created:%3E$(date -v-30d +%F)+stars:%3E500&sort=stars&order=desc&per_page=25"

# Reddit — 커뮤니티 상위 (plain curl은 403, curl_cffi 지문 필요)
python3 -c "from curl_cffi import requests as r; print(r.get('https://www.reddit.com/r/LocalLLaMA/hot/.rss', impersonate='safari').text)"
```

### 수록 기준
- **소문·유출·예고는 뺀다.** r/singularity 상위권 절반이 이런 것들이다. 화제성 신호로만 쓰고 본문에 쓰지 않는다.
- **벤더 발표 수치는 그렇다고 표시한다.** "회사 발표 기준"이라고 적고, 자체 검증 전에는 근거로 쓰지 말라고 덧붙인다.
- **모델 이름은 최소한으로.** 주 단위로 낡는다. 구조적 서술("플래그십은 100만 토큰이 표준")로 바꿔 쓸 수 있으면 그렇게 한다.
- 새 범주가 생겼으면 `terms.js`에도 용어로 넣는다. 섹션과 사전이 따로 놀면 안 된다.

## 윤문 검증 (발행 전)
새로 쓴 항목은 기존 본문과 **밀도로** 비교한다. 분량이 다르니 절대 개수는 의미가 없다.

```bash
# 신규분 vs 기존분 AI 티 밀도 비교 (1만자당)
python3 - <<'EOF'
import json, re, subprocess
def load(p):
    return json.loads(subprocess.check_output(["node","-e",
      "global.window={};require('%s');process.stdout.write(JSON.stringify(window.TERMS))"%p]).decode())
# 비교 기준이 될 이전 버전을 git에서 꺼내 쓴다
# git show HEAD~1:terms.js > /tmp/prev.js
cur = load("./terms.js"); prev = {t["k"] for t in load("/tmp/prev.js")}
new = [t for t in cur if t["k"] not in prev]; base = [t for t in cur if t["k"] in prev]
blob = lambda r: " ".join(" ".join(filter(None,[x.get("d"),x.get("n"),x.get("s")])) for x in r)
C = {"연결어미+쉼표": r"(?:하고|하며|하지만|하면서|해서|이고|이며|되고|되며|있고|있지만),",
     "hype": r"혁신적|획기적|압도적|전례 없는", "진행형": r"고 있다",
     "AI 결산어": r"결론적으로|요약하면|정리하자면|이를 통해",
     "분열문": r"관건은|중요한 것은|필요한 것은", "도치 결산": r"하는 이유다",
     "사전 은유": r"잠식|청사진|적신호|발판", "의의 과장": r"시사하는 바|주목할 만"}
A, B = blob(base), blob(new)
for n, p in C.items():
    a = len(re.findall(p,A))/len(A)*10000; b = len(re.findall(p,B))/len(B)*10000
    print("%-14s 기존 %.1f  신규 %.1f %s" % (n, a, b, "← 손볼 것" if b > max(a*1.3, 1.0) else ""))
EOF
```

**판정 기준**: 신규분 밀도가 기존분보다 30% 이상 높으면서 1만자당 1건을 넘으면 손본다.
문장 길이 표준편차와 종결어미 상위 비율도 같이 본다 — 한 종결이 15%를 넘으면 리듬이 평평해진 것이다.

**오탐 주의**: `-적` 패턴은 의료·법률 정식 용어(후향적 검증, 적응증, 법적 범위)를 잡는다.
숫자만 보고 고치지 말고 실제 문장을 열어볼 것.
