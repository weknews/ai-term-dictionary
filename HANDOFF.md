# 인수인계 — 2026-09-23

## 지금 어디까지 왔나

**위뉴 AI 용어집 v5.0.** 242개 용어 / 12개 분류 / 7개 탭.
2026-09-22 하루 만에 v1.0 → v3.0까지 갔고, 마지막 작업은 모바일 대응이다.

```
index.html   문서 본체 (마크업·스타일·JS). 첫 탭이 오늘의 뉴스  ← 구조는 거의 안 건드림
terms.js     용어 데이터                  ← 용어 추가·수정은 여기만
check.py     발행 전 점검                 ← 배포(Actions)가 자동 호출
CONTRIBUTING.md  집필 원칙 전부. 새 세션은 이것부터 읽을 것
README.md    파일 구조·로컬에서 보기
```

공개 주소: https://weknews.github.io/ai-term-dictionary/ (저장소 weknews/ai-term-dictionary, public)
main에 push하면 `.github/workflows/pages.yml`이 `check.py`를 돌리고 통과할 때만 배포한다.
점검에 실패하면 이전 버전이 그대로 떠 있다 — Actions 탭에서 확인.
아티팩트(https://claude.ai/artifact/XE7BjovdzdW6YDB4HvGMAa)는 예전 공유 경로다. 새 링크는 Pages 쪽을 돌린다.

## 오늘의 뉴스 (2026-09-23 추가)

목적은 구성원(대부분 비개발자)에게 바깥 AI 씬에서 지금 무슨 일이 일어나는지 보여 줘 기획 시야를 넓히는 것이다.
**주제를 미리 정하지 않는다** — 구역·분류를 고정하면 예전에 챗봇·RAG로 정해 둔 것과 같은 함정이 된다.
형식: 헤드라인 · **새로 나온 모델·제품** 3개까지(누가 만든 무엇 · 무엇이 달라졌나 · 어디서 쓰나 — 생각보다 모르는 사람이 많아 헤드라인 바로 아래 카드로) ·
**오늘 눈여겨볼 일** 3~6(무슨 일 + "이 부분을 확인해 보면 좋다") ·
스타트업 렌즈 · 오늘의 용어(개념어, 용어집 정의를 그대로 가져온다).
지시문만 고쳐 다시 써 볼 때: `REDO=1 FROM=write DRY=1 news/run.sh 날짜` (수집·선별 생략, 작문만)
**스타트업 렌즈** 1~2개가 붙는다 — PMF를 찾는 작은 팀의 눈으로 다시 읽는 칸이다(플랫폼이 가져간 것 · 누가 돈을 내나 ·
만든 사람의 기록 · 반대편 신호). 무슨 일인가 → 누가 이득·손해 → 우리 팀에 던질 질문으로 끝난다. 결론 대신 질문으로
끝나야 검증을 통과한다(원문 밖 전망을 막는 장치). 소스: TechCrunch AI · Lenny's · Stratechery · Latent Space · YC 블로그.
용어집은 중립을 지키고, 관점은 이름 붙은 이 칸에만 둔다.
평일 07:30 이 맥의 launchd가 `news/run.sh`를 부른다. API 비용 없이 구독 `claude -p`로 돈다.
hn-researcher(`~/myworks/hn-researcher`, 06:44)가 받아 둔 HN·Lobsters·GeekNews 목록과
`fetch_pages.py`·`gen_report.sh`를 그대로 가져다 쓴다. 모델 턴은 선별·작문 두 번뿐이고 둘 다 도구 없이 1턴이다.

```
collect.py   목록 재사용 + 연구소 피드(OpenAI·DeepMind·HF·Simon Willison·Anthropic) → 코드로 1차 거르기
선별 1턴      prompts/select.md
fetch_pages   원문 확보 (hn-researcher)
작문 1턴      prompts/write.md — 확보한 원문만 근거
verify.py    항목이 수집된 원문을 가리키는지 · 숫자가 원문에 있는지 · 금지 표현 · 원문/댓글 표기
             실패하면 사유를 붙여 고쳐 쓰기 1회, 그래도 실패면 그날은 쉰다
PR           news/DATE 브랜치 → 로컬 검토(news/review.sh)에서 승인 → 병합 → Actions가 check.py 후 Pages 반영
```

- **발행 전 로컬 검토: `news/review.sh [날짜]`** (터미널에서 직접 실행 — 입력을 기다린다). 초안을 발행 화면 그대로 띄운다.
  원문은 각 항목의 링크로 연다. `a` 승인(PR 병합=발행) · `e` 편집기로 수정
  (verify.py를 다시 통과해야 PR에 반영) · `q` 보류. 07:30 초안이 올라오면 맥 알림이 뜬다
- **자동 발행(검토 없이 병합):** `news/work/auto-publish-until`에 날짜를 적으면 그날 호까지는 PR을 만들자마자 병합한다.
  사람이 못 보는 기간용이다. 날짜가 지나면 저절로 PR 검토로 돌아간다. 2026 추석(9/24·9/25)에 처음 썼다(값 2026-09-27).
  끄려면 파일을 지운다. 검증(verify.py)을 통과한 초안만 PR이 되므로 기준은 그대로다
- 설치: `news/install.sh` (launchd 등록). 수동 실행: `news/run.sh [날짜]`, 시험: `DRY=1 news/run.sh 날짜`
- 로그와 중간 산물: `news/work/` (git 미추적, 14일 뒤 삭제). `anthropic-seen.json`은 지우지 말 것 — Anthropic 새 글 판정 기준이다
- **PR 검토에서 볼 것.** 코드는 숫자와 링크만 대조한다. 첫 시험에서 코드가 못 잡은 오류는 전부 해석이었다.
  "같은 모델, 안전장치 수준만 다름"을 "안전장치를 완화한"으로, "프로모션 가격 대비"를 "이전 모델 대비"로,
  기사 속 한 사람의 말을 집단의 일로, 원문에 없는 "위험이 커진다"를 why에 붙였다. 뉴스레터 형식으로 바꾼 뒤에는
  무관한 두 소식을 "정작"으로 엮거나 제품 등급(최상위·직전)을 원문 없이 붙인 경우가 나왔다. 이것들을 먼저 본다.
- 금액은 "3억 5천만 달러"처럼 한국어로 써도 된다. verify.py가 값으로 환산해 원문("$350 million")과 맞춰 본다.
  첫 렌즈 시험에서 숫자가 걸리자 고쳐 쓰기가 한국어 단위로 바꿔 대조를 빠져나간 적이 있어 넣은 규칙이다.
  또 같은 시험에서 기사가 **다른 회사들**을 두고 한 말(매출의 60~70%를 전문가에게 지급)을 렌즈 대상 회사의 일처럼 옮겼다.
  "누구에 대한 문장인가"는 코드로 못 잡으니 검토 때 본다
- PR의 "용어집 후보"는 `terms.js`에 넣을지 검토할 목록이다. 넣을 때는 CONTRIBUTING 출처 규칙을 따른다.
- 맥이 꺼져 있거나 hn-researcher가 바뀌면 그날은 쉰다. 매일 발행이 보장돼야 하면 Actions + API(월 약 $3.5, Sonnet 5)로 옮긴다.

## 탭 구성

| 탭 | 내용 |
|---|---|
| 오늘의 뉴스 (첫 탭, 첫 화면) | 최신 호 전체. `?d=날짜`로 지난 호. 아래에 2026-09-22 스냅샷(지난 자료) |
| 시작 | 왜 만들었나 · 관찰 시점 · 에이전트 능력 누적 표 · 두 번 돌려보기 · 읽는 법 |
| 상황별 | 여섯 상황에 맞춰 36개 추림. 누르면 사전으로 이동 |
| 용어 사전 | 242개. 전문 검색 + 분류·표시 필터 |
| 유행의 역사 | 2022말~2026 시기별 열기와 "그때 → 지금" |
| 더 정확하게 | 바꿔 말하기 표 10줄 |
| 갱신 | v1.0~v3.0 이력 |

## 모바일 개선 — v3.1에서 처리

- 탭: 620px 이하는 두 글자 라벨(`.tab .short`)로 한 줄. 원래 라벨은 화면 읽기용으로 숨겨 남김. 621~760px은 여백만 줄임
- "더 정확하게" 표: 760px 이하에서 행마다 카드로 쌓음 (`td::before` 라벨)
- 필터 패널: 최신 빌드에서 900px 이하 접힘 확인. 스크린샷은 캐시였던 것으로 보임
- 검색창: 620px 이하에서 JS로 짧은 placeholder
- 실기기(iPhone)로는 아직 안 봤다. Chrome 390/606/700/1440 폭으로만 확인

### 확인 방법
```bash
python3 check.py && python3 -m http.server 8750
```
Chrome 도구로 창을 390×844로 리사이즈해 확인한다. 리사이즈가 안 먹는 경우가 잦다(innerWidth가 110 같은 값으로 나옴).
그럴 땐 같은 출처 iframe을 원하는 폭으로 만들어 재면 된다. 단 iframe은 첫 로드 때
matchMedia가 크기 반영 전에 평가되니 한 번 reload한 뒤 값을 볼 것. 390 / 606 / 1440 세 폭에서 본다.

## 그 외 남은 일

- 의료 용어는 v2.8(14개)과 v4.0(28개)에 걸쳐 전부 뺐다. 원문은 git 이력에 있다
  (`git show 0951b15:terms.js`, v4.0 직전 커밋의 `terms.js`). 별도 자료로 뽑을 때 거기서 가져온다.
- 이 문서의 목적은 사내 구성원이 AI 용어를 이해하고 흐름을 따라가게 하는 것이다.
  특정 산업·회사 관점은 넣지 않는다(CONTRIBUTING 수록 범위).
- 첫날 만든 ELI5 페이지가 따로 있다 — https://claude.ai/artifact/9o1JzFDYRcmuLUrnwt5aCX
  밤티·발레코어 비유가 그대로 들어 있고 본 용어집과 연결돼 있지 않다. 정리 여부 미정.

## 반드시 지킬 것 (여러 번 지적받은 것들)

`CONTRIBUTING.md`에 전부 적혀 있다. 그중 실제로 사고가 났던 것:

1. **출처 없이 쓰지 않는다.** 2025년 이후 등장 용어(66개)는 정의를 쓰기 전 반드시 검색한다.
   `그래프 엔지니어링`을 특정 도구 얘기로 좁혀 쓴 것, `에이전트 함대`를 스웜 개념으로 쓴 것이
   모두 확인 없이 아는 대로 써서 난 사고다. 이걸로 크게 지적받았다.
2. **새 내용을 쓰면 `python3 check.py`를 돌린다.** 규칙을 문서에 적어두고 정작 안 돌려서 지적받았다.
   지금은 배포가 강제하지만, 문구만 고칠 때도 올리기 전에 돌린다.
3. **사람을 깎아내리지 않는다.** "까인다", "촌스럽다", "아마추어" 전부 걷어냈다.
   문서 구조도 표현이다 — "핵심만"이라는 탭 이름이 읽는 사람을 낮춰 본다고 지적받아 "상황별"로 바꿨다.
4. **같은 말을 버릇처럼 쓰지 않는다.** "자리"를 41번 썼다가 지적받았다. `check.py`가 빈도를 센다.
5. **검출 숫자를 그대로 믿지 않는다.** `-적`은 의료·법률 정식 용어를 잡고,
   연결어미 뒤 쉼표는 정상 쉼표까지 잡는다. 문장을 열어보고 고친다.
6. **AI 용어집이다.** AI가 없어도 원래 있던 말은 넣지 않는다.

## 글의 성격

- 평서형 "~다" 체. 경어체로 바꾸지 않는다.
- 정의(d)는 사실만, 뉘앙스(n)는 판단 허용, 현황(s)은 관찰 날짜가 붙는 줄.
- 시점에 따라 달라지는 서술은 `d`나 `n`이 아니라 `s`에만 적는다.
- 사용자는 번역투와 상투어에 민감하다. 윤문 기준은 CONTRIBUTING의 표현 원칙을 따른다.
